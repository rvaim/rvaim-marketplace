{
  "name": "letta-mem",
  "version": "2.2.1",
  "description": "通过本机或自托管新版 Letta App Server 为 Claude Code 与 Codex 提供工作区与自主共享持久记忆",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify": "npm run check && npm test && npm run build"
  },
  "dependencies": {
    "@letta-ai/letta-agent-sdk": "0.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.17.0",
    "esbuild": "^0.25.8",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
