{
  "name": "letta-mem",
  "version": "2.3.0",
  "description": "通过 Letta Code Agent MemFS 与原生 Shared Memory 为 Claude Code 和 Codex 提供持久记忆",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify": "npm run check && npm test && npm run build"
  },
  "dependencies": {
    "@letta-ai/letta-agent-sdk": "0.6.0",
    "@letta-ai/letta-code": "0.30.0"
  },
  "devDependencies": {
    "@types/node": "^22.17.0",
    "esbuild": "^0.25.8",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
