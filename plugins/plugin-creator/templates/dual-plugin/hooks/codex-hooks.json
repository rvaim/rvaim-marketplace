{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|apply_patch",
        "hooks": [
          {
            "type": "command",
            "command": "node ${PLUGIN_ROOT}/bin/{{HOOK_SCRIPT_NAME}}.mjs",
            "timeout": 30,
            "statusMessage": "正在检查"
          }
        ]
      }
    ]
  }
}
